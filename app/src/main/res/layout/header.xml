<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="@color/green">

    <androidx.cardview.widget.CardView
        android:id="@+id/nav_profilePicContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        app:cardCornerRadius="200dp"
        app:layout_constraintBottom_toTopOf="@+id/nav_fullName"
        app:layout_constraintEnd_toEndOf="@+id/nav_fullName"
        app:layout_constraintStart_toStartOf="@+id/nav_fullName">

        <ImageView
            android:id="@+id/nav_image"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:background="@drawable/circle_shape_image" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/nav_fullName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:textColor="@color/grayLight"
        app:layout_constraintBottom_toTopOf="@+id/nav_email"
        app:layout_constraintEnd_toEndOf="@+id/nav_email"
        app:layout_constraintStart_toStartOf="@+id/nav_email" />

    <TextView
        android:id="@+id/nav_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginBottom="16dp"
        android:textColor="@color/grayLight"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>